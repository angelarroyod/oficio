import { PlaceholderScreen } from '@/components/PlaceholderScreen';
import { copy } from '@/lib/copy';

export default function ClientRequestsScreen() {
  return <PlaceholderScreen description={copy.placeholders.clientRequests} />;
}
