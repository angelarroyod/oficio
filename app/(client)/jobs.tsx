import { PlaceholderScreen } from '@/components/PlaceholderScreen';
import { copy } from '@/lib/copy';

export default function ClientJobsScreen() {
  return <PlaceholderScreen description={copy.placeholders.clientJobs} />;
}
