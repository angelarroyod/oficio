import { PlaceholderScreen } from '@/components/PlaceholderScreen';
import { copy } from '@/lib/copy';

export default function ClientHomeScreen() {
  return <PlaceholderScreen description={copy.placeholders.clientHome} />;
}
